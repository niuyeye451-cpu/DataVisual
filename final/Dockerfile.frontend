FROM node:20-alpine

WORKDIR /app

COPY srtp/dashboard/package.json srtp/dashboard/package-lock.json ./
RUN npm install

COPY srtp/dashboard/ ./

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
